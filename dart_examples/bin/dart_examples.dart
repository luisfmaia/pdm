import 'package:dart_examples/input_output.dart';
import 'package:dart_examples/types.dart';
import 'package:dart_examples/control.dart';
import 'package:dart_examples/functions.dart' as func;

void main(List<String> arguments) {
  /* IO */
  // output();
  // inputString();
  // inputNumber();

  /* TYPES */
  // vars();
  // numbers();
  // strings();
  // records();
  // lists();
  // sets();
  // maps();
  // var f = functions();
  // print(f(5));

  /* CONTROLS */
  // loops();
  // branches();

  /* FUNCTIONS */
  // print('Hello world: ${func.calculate()}!');
  // print('O número 7 é par? ${func.ehPar(7)}');
  // print('O número 0 é impar? ${func.ehImpar()}');
  // print('O número 11 é impar? ${func.ehImpar(11)}');
  // print(func.formataEndereco('Aarão Reis', 'Centro', 'Caxias'));
  // print(func.formataEndereco(
  //     'Afonso Pena', 'Centro', 'Caxias', '123', '65.600-000'));
  // print('As raízes da equação 2x^2+4x+2 são: ${func.calcularRaizes(2, 4, 2)}.');
  // func.anonimas();
  // func.aninhadas();
}
